[project]
name = "open-cpap-parser"
version = "0.1.0"
description = "Multi-manufacturer CPAP data parsing library and CLI tool"
authors = [{ name = "Camden Bock", email = "camdenbock@gmail.com" }]
license = "GPL-3.0-only"
readme = "README.md"
requires-python = ">=3.11"
keywords = ["cpap", "sleep-apnea", "medical", "resmed", "respironics", "data-parsing"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Healthcare Industry",
    "License :: OSI Approved :: GNU General Public License v3 (GPLv3)",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Rust",
    "Topic :: Scientific/Engineering :: Medical Science Apps.",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
    "cpap-py>=1.0.0",
    "pydantic>=2.0.0",
]

[project.urls]
Homepage = "https://gitlab.com/open-cpap/open-cpap-parser"
Repository = "https://gitlab.com/open-cpap/open-cpap-parser"
Issues = "https://gitlab.com/open-cpap/open-cpap-parser/-/issues"

[project.optional-dependencies]
respironics = ["pyedflib"]
docs = [
    "mkdocs-material>=9.0",
    "mkdocstrings[python]>=0.25",
]

[project.scripts]
cpap-parser = "open_cpap_parser.cli:main"

[build-system]
requires = ["maturin>=1.0"]
build-backend = "maturin"

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "open_cpap_parser._rust_parsers"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
